{
  "name": "parcel-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "copy": "rm -rf dist/ && mkdir dist && mkdir dist/images && cp -R src/assets/images/svg dist/",
    "dev": "npm run copy && parcel app/index.html",
    "build": "npm run copy && parcel build app/index.html && cp _redirects dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.12.0",
    "parcel-bundler": "^1.12.3",
    "sass": "^1.21.0"
  }
}
